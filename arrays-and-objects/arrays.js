const fruits = ["Apple", "Banana", "Cherry"];

// console.log(fruits); // Expected output is [Apple, Banana, and Cherry]

// console.log(fruits[2]); // This will return the fruit at the second index which should be Cherry

// fruits[1] = "Raspberry"; //this will change the fruit at the 1st index (banana) to raspberry

// console.log(fruits);


// Array methods

// fruits.push("Blueberry"); // This will push a new fruit into the array by stacking - adds to end

// console.log(fruits); // adding the .length will show updated amount of 4 in the array

// fruits.pop(); // removed the pushed data from the array - removes from end

// console.log(fruits); 

// fruits.shift(); // removes index 0 and shifts everything down - removes from beginning

// console.log(fruits);

// fruits.unshift("Limes"); // adds to index 0 and shift everything up - adds to beginning

// console.log(fruits);

// // splice - adds or removes elements. Multi-tool for adding and removing

// fruits.splice(1, 1, "Kumquats");

// console.log(fruits);

// fruits.splice(1, 2);

// console.log(fruits);


// const numbers = [1, 2, 3, 4, 5];

// // map 

// console.log("numbers =", numbers);
// const doubled = numbers.map((num) => num * 2);
// console.log("doubled =", doubled);

// // filter

// const evenNumbers = numbers.filter(num => num % 2 === 0);
// console.log(evenNumbers);



